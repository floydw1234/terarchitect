# Containerized Worker Runtime Contract

- Validate `codex`, `ah`, Python imports, repo test tooling, and writable config mounts inside the worker runtime.
- Confirm multi-commit AgentHub publish flows verify ancestry rather than assuming a single child commit.
- After runtime fixes, retry from a clean queue state and re-check the produced attempt artifacts.
